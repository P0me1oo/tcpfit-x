# 更新记录

## 0.18.1

- 通用接入入口改为单行命令，Linux / OpenWrt / iStoreOS Shell 和 Windows PowerShell 5.1/7 均可粘贴后按一次回车执行，无需安装启动器。
- 保留同版本脚本下载、临时配对、curl/wget 回退和执行失败的退出码；同步单行格式检查、使用说明、运行模块版本与发布校验清单。

## 0.18.0

- 菜单的 `Network` 行增加 TCP 接收、发送缓冲区上限，直接读取内核当前生效值，按 MiB、KiB 或字节显示；单项读取失败显示 `?`。
- 缩短通用接入命令：每个平台只保留一个带临时 token 的下载地址，由服务器动态补齐接入参数。保留 Linux / OpenWrt / iStoreOS 和 Windows PowerShell 5.1/7 的同段命令接入，Windows 响应带 UTF-8 BOM，兼容中文系统的解码。
- 短链接检查 token 有效期和配对状态，下载不消耗配对机会；Linux 只有完整下载成功才执行，curl 失败时丢弃残缺内容再回退 wget。
- 更新接入回归、使用说明、运行模块版本和发布校验清单。

## 0.17.0

- 菜单 2 和双端协议检查统一输出一段多行接入命令。Linux / OpenWrt / iStoreOS Shell 与 Windows PowerShell 5.1/7 可完整复制同一段内容，自动执行对应客户端，无需手选系统或增加运行依赖。
- 保留同版本脚本下载、四个接入参数和两端客户端行为；补充通用入口的真实 HTTP 下载、退出码、Windows 整段执行与逐行输入配对回归，同步使用说明、运行模块版本和发布校验清单。

## 0.16.2

- 优化线路调优日志将“步长”改为收发缓冲区上限“各增加／减少”的实际容量，明确调整对象；同步开始试调、回退提示和 README 示例，说明 MiB 与测速 Mbps 的含义。
- 删除启动时的 HTTP／防火墙说明、带宽参考与端口规则摘要，以及端口日志中的退出清理说明。
- 同步现有日志断言、运行模块版本和发布校验清单。

## 0.16.1

- 优化线路调优每次评估的测速次数允许设为 1 次，范围由 2～10 次调整为 1～10 次，默认仍为 2 次；交互输入、命令行参数和运行前校验保持一致。
- 更新次数校验、单次测量记录与结果判定回归，说明单次测速无法比较同组波动，并同步运行模块版本和发布校验清单。

## 0.16.0

- 优化线路调优初始收发上限改为 `1.5 × BDP`，取消固定 2 MiB 余量和 6 MiB 下限；后续按 2 MiB 上探，最高到 `2.5 × BDP`。该模式不再沿用按总内存计算的 24 MiB、256 MiB 等缓冲区上限，普通基础调优公式和内存范围保持原状。
- 重传不超过 1%、测速稳定且速度相对固定参照下降不超过 5% 时，优先保留更大的缓冲区，不再要求提速 3% 或测速区间完全分开。重传在 1% 内上升也可以保留，最终验收和队列变化后的复测使用相同门槛。
- 高重传、明显波动或降速超过保护范围的候选回退，继续搜索；保留高重传边界下方 1 MiB 微调、固定速度参照、失败恢复及不限轮数的规则。初值和上限按整数字节向下取整，默认值与 TCP 最小值适配小缓冲区。
- 补充公式、低 BDP、精确上限、速度不变仍保留、重传阈值和最终验收回归，更新使用说明、运行模块版本及发布校验清单。

## 0.15.0

- 优化线路调优在初值测速完整但波动明显时继续试调，以首次稳定候选建立固定比较参照；首次稳定候选重传尚未优秀时也会继续调整。最终验收与保存使用同一参照，避免再次因初值不稳定而回退；测速失败或数据缺失仍停止并恢复。
- 取消最多 8 轮、连续 3 轮无收益和 30 分钟总时限。无收益候选回退后继续搜索，附近上限测完后继续寻找更远的未测候选，直到当前可调范围或高重传边界下方没有候选。保留单次测速超时、断线和中断恢复。
- 记录建立稳定参照的候选和轮次；所有候选都不稳定时，搜索结束后恢复并推荐原配置，完整保留候选测速记录供手动选择。
- 更新入口文案、逐轮日志、使用说明和回归测试，运行模块统一为 0.15.0。

## 0.14.0

- 优化线路调优取消原始基线测速，先单独采集空载 TCP 握手延迟，再计算并应用 `BDP + 2 MiB` 初值。首次单连接测速作为后续速度比较的参照，Linux 和 Windows 测速端均支持纯延迟采集。
- 删除试调结束后的独立单连接验收，直接采用已保留配置的测速结果；实际参数没有变化时不再复测，已有整形或队列变化后的验证与失败恢复继续保留。
- 结果表按相同实际参数合并重复测速，展示速度与重传比中位数、简短判定和普通序号。按配置序号选择保存，回车使用推荐配置；逐次数据和完整快照继续存档。
- 临时解除限速前显示当前总限速和单连接限速，无限制时显示“未设置”。精简测速方向、缓冲区推导和结果报告中的重复说明。
- 更新流程、配置选择、延迟协议与两端客户端回归，同步使用说明、运行模块版本和发布校验清单。

## 0.13.1

- 修正优化线路调优的上探步长：单连接重传优秀时按 2 MiB 增大候选上限；更高候选出现稳定高重传（`>1%`）后，从该候选按 1 MiB 向下微调。
- 记录已测高重传边界，微调不越过边界继续上探，不重复已测上限；失败候选先恢复上一保留配置，再验证微调值。微调只测试高于保留上限的未测候选，收益判定、轮数和时间限制保持原规则。
- 日志区分保留配置与候选结果，并标明微调参考候选。同步步长、阈值边界、微调回退和完整流程回归、说明文档、运行模块版本及发布校验清单。

## 0.13.0

- 优化线路调优在单连接重传优秀后继续上调缓冲区，候选上限按 1 MiB 递增；基础候选已达标也会试探提速。重传未达优秀时沿用下调、缩步和反向细调。
- 新增稳定提速收益：速度中位数相对上一保留组至少提高 3%，且候选每次速度均高于上一组最高速度；重传须保持优秀且未明显变差。重传已优秀时，仅进一步降低重传不再作为上调收益。无收益候选回退后可继续尝试更高未测上限，保留 8 轮、连续 3 轮无收益和任务时限。
- 删除基础候选和每轮试调前后的可用内存、系统预留余量及缓冲区增量预算检查，移除对应日志与结果字段；按机器总内存计算的试调上限保持原规则。
- 缓冲区摘要只显示收发上限，移除“起始值”和基础推导中的 `Buffer default` 输出；实际默认值、配置存档与恢复保持原规则。
- 补充上调提速、测速波动、回退后继续探测和完整流程回归，同步说明文档、运行模块版本及发布校验清单。

## 0.12.0

- 优化线路调优新增 Windows 测速端，支持原生 PowerShell 5.1/7。调优端分别生成 Linux 和 Windows 接入命令，提供同版本 PowerShell 脚本下载，沿用一次性配对、来源绑定和版本检查。
- Windows 优先使用现有 `iperf3.exe`，缺少时通过 WinGet 安装 `ar51an.iPerf3`；兼容安装后 PATH 尚未刷新，也可用 `-IperfPath` 指定程序。接入前检查程序可用性，缺少条件或安装失败时明确停止。
- Windows 接入现有 IPv4/IPv6、单连接和四连接反向下载、空载和满载 TCP 握手延迟采集、心跳与结果回报。增加当前用户任务锁和 Windows 作业对象清理，测速失败不回报成功结果，强制关闭时结束子进程。
- 安装、更新、模块检查和发布校验清单纳入 `tcpfit-client.ps1`；保留 UTF-8 BOM 以兼容 PowerShell 5.1 中文。补充 Windows 原生回归、使用说明与双端验证记录。

## 0.11.0

- 仅调整优化线路调优：TCP 收发缓冲区初始上限改为 `BDP + 2 MiB`，统一使用 6 MiB 下限；后续试调也遵守该下限。保留现有内存试调上限，低于 6 MiB 时明确停止并恢复配置。普通基础调优公式与下限保持原状。
- 缓冲区基线、候选、试调和独立验收只测单连接，删除四连接重传调优、优先顺序与阶段切换。保留单连接优秀 `≤0.5%`、高重传 `>1%`、原始基线 5% 速度保护、最多 8 轮和连续 3 轮无收益停止规则。
- 优化线路调优填写任意一端标称带宽时，BDP 使用已填值中的较小值，不额外探测；两端都留空时自动用四连接探测当前路径带宽。探测不稳定、不完整或低于可推导范围时停止并恢复配置。空载 RTT 继续使用单连接基线采集的 TCP 握手延迟。
- 已有全局整形的提高检查、候选验证与提高后的最终验收保留四连接，并在最终配置下检查单连接表现；提供标称带宽且无全局整形时，全程只测单连接。
- 同步交互提示、使用说明、回归测试、运行模块版本和发布校验清单。

## 0.10.1

- 优化线路调优默认固定使用接入端口 TCP 12223、测速端口 TCP 12224，交互和 `--yes` 均不询问端口；端口被占用时明确退出，不自动更换。
- 保留双端口并行接入与测速、空载和满载延迟采样、本机防火墙临时放行及结束或 Ctrl+C 后的清理。
- 未检测到防火墙工具时，提示跳过规则管理并继续测试，不将工具缺失表述为端口被拦截。
- 同步入口回归、运行模块版本、使用说明与发布校验清单。

## 0.10.0

- 优化线路调优不再询问端口，默认由系统选择两个不同的空闲 TCP 端口，实际绑定后保留到对应服务启动。接入服务直接接管预留端口，启动失败或中断时释放预留。
- 启动时显示实际使用的接入和测速端口，并写入任务结果；原有端口参数继续兼容，省略或传 `0` 自动选择。
- 明确本机防火墙的临时放行行为：先允许接入，配对后向该来源开放测速，结束或 Ctrl+C 后撤销本次规则、停止服务并释放端口；增加清理完成提示。
- 增加自动选端口、已有端口占用、IPv4/IPv6、服务接管、中断释放和无端口交互回归，扩展 Linux 隔离测试检查两个端口临时放行及撤销、等待接入和测速中的 SIGINT 清理。
- 运行模块和文档升级为 0.10.0，并更新发布校验清单。

## 0.9.1

- 统一菜单名称：菜单 `1` 为“国际线路调优”，菜单 `2` 为“优化线路调优”；同步英文名称和菜单列宽。
- 同步修改启动、帮助、测速端、结果报告、安装更新、恢复提示及文档中的相关描述。
- 命令、参数、脚本文件名、存档格式和调优逻辑保持兼容；运行模块统一升级为 0.9.1，并更新发布校验清单。

## 0.9.0

- 优化线路调优缓冲区初值改为 `2 × BDP`，移除固定 2 MiB 余量；沿用当前带宽参考、空载 RTT 和内存约束，普通基础调优公式保持原状。
- 每种连接数每次评估默认测 2 次，新增 `--repeats 2..10` 和交互输入校验；逐次保存实际接收速度、发送端重传及完整配置快照，明显波动标记不稳定。
- 按单连接优秀 `≤0.5%`、高重传 `>1%`，四连接优秀 `≤1%`、高重传 `>5%` 分两阶段调节。高重传优先下调 1～2 MiB，每轮只测当前模式；达标即切换，切换后重新测当前参数。
- 效果变差则回退、缩步和必要的反向细调，达标后停止加缓冲区；两种速度分别与原始基线比较，下降容差为 5%。两阶段合计最多 8 轮，连续 3 轮无收益停止。
- 试调结束后独立复测单连接和四连接，检查第二阶段对先前模式的影响；保留内存检查、任务时限和异常恢复。
- 保存前显示全部逐次记录，包含回退、失败、跳过及未完成验证状态；标明自动推荐和同配置编号。回车保存推荐配置，也可按有效测速序号保存当时完整配置，`--yes` 自动保存推荐。
- 同步优化线路调优说明和测试，保留既有历史实测记录。三份运行模块统一升级为 0.9.0。

## 0.8.1

- 将自动缓冲区初始公式调整为 `1.5 × BDP + 2 MiB`，保留固定余量、4 MiB 下限和各内存档的试探上限；普通基础调优与优化线路调优共用该公式。
- 同步推导日志、持久化配置注释和使用说明，补充倍率、整数取值及内存边界回归。
- 修正优化线路调优开始页仍显示最多三轮的旧提示，与实际八轮上限、连续三轮无收益停止保持一致。
- 修复内核自动创建的 `fq 0:` 队列在恢复时被重复修改而报错：参数已经一致时保留队列，需要改变参数时使用完整重建；补充默认队列和参数差异回归。

## 0.8.0

- 将 512 MiB 档机器的自动 TCP 收发缓冲区试探上限提高到各 24 MiB；兼容内核保留内存后的可见容量，保留更小机器的原比例、256 MiB 绝对上限和原 TCP 全局内存预算。初值仍由 BDP 推导，最终配置由优化线路调优实测决定。
- 优化线路调优试调改为低重传上调 2～4 MiB、高重传下调 1～2 MiB；候选未通过时恢复上一组参数、缩小步长，允许反向细调，不重复测试相同上限。
- 同时比较三次实际接收速度和估算重传比的中位数：速度允许 5% 波动，综合速度提高至少 3% 或重传明显下降才保留新候选，并阻止多轮小幅速度损失累积。高重传正在改善时不再因仍高于 1% 被误判为重复跳变。
- 增加可用内存和四连接收发缓冲区增量预算检查；最多追加八轮、连续三轮无收益停止，保留整任务 30 分钟超时及异常恢复机制。
- 每轮显示调整依据、步长、前后参数、接收速度、估算重传比和保留／回退结果；记录内存、失败恢复和停止原因。新增搜索、波动、内存限制及整任务失败恢复回归。
- 快照恢复时核对缓冲区实际值，普通退出、异常退出守护恢复和手动恢复共用此检查；不一致时保留待恢复记录，不报告恢复完成。

## 0.7.0

- 优化线路调优显示缓冲区推导依据、修改前后数值、内核实际值和每组测速的吞吐、估算重传比中位数。
- 增加优化线路调优缓冲区试调：按重传表现向上或向下调整约 25%，沿用原版内存范围，最多追加三轮；每轮实测决定暂用或恢复，最终仍与调优前配置比较。
- 新增 `tcpfit buffer --max-mb N [--default-mb N]`，自定义收发缓冲区上限及默认值，立即应用并持久化，修改前后自动存档。
- 缓冲区应用后核对实际值，失败或普通中断时恢复原值；修改持久化配置时保留其他参数。基础推导同步约束默认值不超过上限。
- 新增逐轮参数与测量记录，最终报告展示实际保留的缓冲区；更新使用说明、耗时估计及缓冲区和流程回归验证。

## 0.6.1

- 优化线路调优自动获取服务器公网地址：优先读取实际出站路由的源地址，必要时查询公网出口，成功后不再要求手填；只有获取失败才提示填写。
- 地址探测遵守 `-4`、`-6`；未指定协议族且仅有 IPv6 时自动使用 IPv6。保留 `--server` 指定地址，`--yes` 探测失败时明确退出。
- 主菜单统一使用连续数字：`1` 国际线路调优、`2` 优化线路调优，其他选项顺延至 `11`，`0` 退出。同步修正菜单跳转、操作提示和文档。
- 补充地址探测、失败回退和菜单操作对应关系的回归验证，安装测试的版本样本从当前源码版本生成。

## 0.6.0

基于上游 tcpfit 0.5.7，由 P0me1oo 维护本分支。

- 新增菜单 `r` 和 `tcpfit return`，独立执行优化线路调优，无需先运行公共节点调优。
- 新增基于 iperf3 的家宽测速脚本，支持常规 Linux、OpenWrt、iStoreOS；家宽主动执行单连接和四连接反向下载。
- 接入命令通过 curl 或 wget 从调优端下载同版本脚本，只传递服务器地址、两个端口和临时 token；脚本下载、配对、控制指令和结果回报使用明文 HTTP。
- 新增一次性限时配对、单测速端会话来源绑定和任务结束清理，不要求 OpenSSL 命令行工具，不生成临时证书。
- 防火墙只复用已有工具，兼容已启用的 UFW、原生 nftables 和 iptables/ip6tables；不安装防火墙软件，不启用原本关闭的 UFW，也不修改 UFW 配置文件。
- 有可用防火墙工具时，临时测速端口只允许已配对的来源 IP；没有工具时继续任务，并在确认页和结果中记录未启用端口来源限制。测速端不需要防火墙工具。
- 依赖准备只补装缺少的运行和测速工具，不额外请求证书包；Debian 使用 `--no-install-recommends`。
- 复用原版带宽探测、基础参数推导、完整基础调优、重传判据和验证时长；自动采集空载与满载 TCP 握手延迟。
- 优化线路调优模式不做完整整形扫描，不新建或降低全局整形。已有上限只有在稳定超出、单个提高候选通过复测时才提高。
- 国际线路调优与优化线路调优入口使用同一任务锁；重复接入不替换当前测速端。
- 新增完整配置快照、每轮真实结果、最终配置记录、异常退出守护恢复及 `return-recover`。优化线路调优存档可通过原版存档入口恢复。
- 测速端被强制结束时，独立清理进程回收凭据、任务锁和本次测速子进程。
- 安装、更新、卸载同步管理新模块；发布时校验所有运行文件。
- 修正队列恢复中的字节单位、fq_codel 隐含 ECN 关闭状态及 IPv6 自动路由到期时间处理。
- 补充安装与更新、HTTP 配对、IPv4/IPv6 防火墙、UFW 共存与重载、无防火墙双端测速及异常恢复的自动验证。

两端需使用同版本脚本，每次任务使用新生成的接入命令。原有快照、存档和异常恢复记录继续兼容。

支持范围、指标口径与自动保留规则见 [README](README.md)。开发与验证方法见 [TESTING](TESTING.md)。
