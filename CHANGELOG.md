# 更新记录

## 0.6.0

基于上游 tcpfit 0.5.7，由 P0me1oo 维护本分支。

- 新增菜单 `r` 和 `tcpfit return`，独立执行回国调优，无需先运行公共节点调优。
- 新增基于 iperf3 的家宽测速脚本，支持常规 Linux、OpenWrt、iStoreOS；家宽主动执行单连接和四连接反向下载。
- 新增一次性限时配对、HTTPS 公钥指纹认证、来源 IP 限制和任务结束清理。
- 复用原版带宽探测、基础参数推导、完整基础调优、重传判据和验证时长；自动采集空载与满载 TCP 握手延迟。
- 回国模式不做完整整形扫描，不新建或降低全局整形。已有上限只有在稳定超出、单个提高候选通过复测时才提高。
- 原版与回国入口使用同一任务锁；重复接入不替换当前测速端。
- 新增完整配置快照、每轮真实结果、最终配置记录、异常退出守护恢复及 `return-recover`。回国存档可通过原版存档入口恢复。
- 测速端被强制结束时，独立清理进程回收凭据、任务锁和本次测速子进程。
- 安装、更新、卸载同步管理新模块；发布时校验所有运行文件。
- 修正队列恢复中的字节单位、fq_codel 隐含 ECN 关闭状态及 IPv6 自动路由到期时间处理。

支持范围、指标口径与自动保留规则见 [README](README.md)。开发与验证方法见 [TESTING](TESTING.md)。
