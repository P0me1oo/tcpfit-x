# 开发与验证

## 本地回归

需要 Python 3 和 Bash。Windows 可使用 Git for Windows 的 Bash。

```bash
python -X utf8 -m unittest discover -s tests -v
bash -n tcpfit.sh
bash -n install.sh
sh -n tcpfit-client.sh
```

常规回归检查真实结果解析、错误与不完整数据拒绝、HTTP 接入、一次性配对、来源绑定、心跳中断、防火墙选择、优化线路调优单连接重传阈值、已有整形的四连接验证条件和路由恢复。入口回归覆盖服务器地址自动探测、IPv4/IPv6、外部查询失败、手动回退及数字菜单与实际操作的对应关系；使用替代命令，不请求外网或修改网络。依赖测试使用替代包管理器检查实际安装请求，不执行软件安装。Linux 集成测试默认跳过，不会在普通开发机上修改网络。

Windows 会分别用已安装的 PowerShell 5.1 和 7 运行 `tests/test_windows_client.py`，需要系统的 .NET Framework C# 编译器生成临时替代程序。测试执行 Windows 单行接入命令，使用仅包含临时测速工具的 PATH，验证无需 Shell、curl 或 Python；使用真实本地 HTTP、套接字和子进程，检查脚本下载与中文编码、IPv4/IPv6 配对、单连接和四连接下载参数、纯空载延迟采集、延迟回报、无效指令与测速失败、WinGet 安装后查找，以及强制关闭后的进程和任务锁清理。接入命令须只下载一次 PowerShell 脚本，绕过不可用的默认代理，并在默认错误处理方式下拒绝过期下载；通过标准输入传入时，一次回车即可配对。纯延迟任务必须回报有效样本且不启动测速子进程。替代程序只生成测试结果，不进行大流量测速或实际安装；缺少对应 PowerShell 或编译器时跳过。可以单独执行：

```powershell
python -X utf8 -m unittest discover -s tests -p test_windows_client.py -v
```

端口回归使用真实本地套接字，验证 IPv4/IPv6 端口保留、显式端口被占用时拒绝启动、部分绑定失败和中断会释放预留、HTTP 服务接管预留套接字并正常提供脚本、关闭后两个端口可重新绑定。入口测试检查交互和 `--yes` 均固定默认使用 12223 / 12224 且不询问端口；原有端口参数与显式传 `0` 自动分配的行为继续兼容。

队列恢复回归还检查内核自动创建的零句柄队列：删除临时候选后，自动队列参数与快照相同就保留；参数不同时完整重建，避免 `tc qdisc change` 无法寻址 `fq 0:` 而误报恢复失败。

缓冲区回归以临时文件模拟内核参数，执行真实配置写入与失败恢复，覆盖自定义上限和默认值、单位换算、无效输入、现有参数保留、内核拒绝或截断、保存失败和中断。优化线路调优试调使用模拟测量验证以下行为：

- 优化线路调优 `1.5 × BDP` 初值和 `2.5 × BDP` 上限，取消固定余量、6 MiB 下限及按内存计算的缓冲区上限；覆盖低带宽、低延迟、整数字节取整、默认值收小、超过原 256 MiB 范围和内核整数范围拒绝。后续下调受实际 TCP 最小值约束。普通基础调优仍使用 `1.5 × BDP + 2 MiB` 和 4 MiB 下限，512 MiB 档限制、相邻档单调性、角色默认值和 TCP 全局内存预算保持原规则。
- 单连接重传 `≤1%` 可上调，`>1%` 才触发高重传微调，`≤0.5%` 保留优秀标记；四连接重传数据不触发缓冲区试调，也不参与缓冲区验收。
- 每次评估默认测 2 次，命令行和交互可设 1～10 次；验证单次测量按完整结果记录、参与判定，不会被误报为未完成。初值和后续试调均只测单连接。先采集空载延迟，再计算并应用初值，之后才开始单连接测速；没有原始基线或试调结束后的独立验收。
- 填写任意一端标称带宽时跳过带宽探测，BDP 使用已填值中的较小值；两端都留空时自动用四连接探测。验证探测次数、带宽取整、不混用带宽探测与独立空载采样的延迟，以及数据不稳定、不完整或过低时恢复原配置；无效标称值仍拒绝启动测速。空载采样使用有效均值的中位数，无有效值时明确采用原有默认估值。
- 重传不高后按 2 MiB 向上试探更大缓冲区；初值已经合格时直接上调。更高候选出现高重传后，从该候选按 1 MiB 向下微调，不越过已知高重传边界。验证上探、回退、微调的实际参数顺序、跳过已测候选，并直接使用已保留组的结果。初值重传偏高或不稳定时，下调、缩步后继续寻找更远候选，再尝试反向。最后一步精确落在 `2.5 × BDP` 上限，不向上取整，不设轮数或连续无收益次数限制。
- 更大的候选在速度不变、小幅下降或测速区间重叠时仍可保留；重传从 0% 增至 0.8% 或恰好 1% 可以通过，超过 1% 则回退。保留测速稳定性及固定参照的 5% 降速保护，不再要求提速 3%。验证无提速上探到最大值、完整流程保存和队列变化后的最终验收，避免旧的相对重传规则退回合格候选。
- 单连接速度相对固定稳定参照保护 5%，单次明显波动标记不稳定，重传或速度缺失不能被当成正常值。完整但不稳定的初值继续试调，首次稳定候选建立参照；即使该候选仍有高重传，也能继续优化。验证日志中的 608.43 / 405.65 Mbps、0% / 5.366% 波动不会直接结束任务，连续小幅降速不能突破首个稳定参照的 5% 保护。采样失败或数据缺失仍停止并恢复任务前配置。最终参数不变时复用保留组的测速记录，不新增测速序号。
- 连续三轮无收益后仍能找到后续收益，稳定改善可继续超过八轮；尚未稳定时附近邻点测完后继续探索更远范围。全部候选都不稳定时先搜索至无可用候选，再推荐原配置并保留候选供手选；已经取得稳定参照的最终保存与队列复测使用该参照，不再因初值波动而回退。
- 提供标称带宽且无全局整形时全程只测单连接；已有整形时，在实际保留的缓冲区下独立测四连接，用于提高检查和候选验证。提高后的最终配置复测单连接和四连接；四连接候选失败时恢复原整形。
- 每次测量保存完整配置关联，交互表按实际 TCP、队列和路由参数合并为普通序号；各连接数分别统计速度和重传中位数。存档时间、文件或服务状态变化不拆分实际参数相同的结果，缓冲区相同但拥塞控制或限速不同的配置不得合并。按合并后的序号选择，回车和 `--yes` 使用推荐；只有失败或跳过记录的配置不能手选。
- 原始 iperf3 校验失败仍保存两端数据；截断数据、失败和未执行项目保留状态，缺失重传不填零。手选恢复完整参数，并同步持久化参数与队列。
- 完整流程不读取可用内存或检查预留余量，日志不显示缓冲区默认值；优化线路缓冲区范围仅由 BDP 推导，不设 30 分钟总时限。模拟配对满一小时且心跳正常时继续工作，随后心跳超时仍明确失败。测速失败、心跳中断、参数部分应用失败和普通中断先恢复上一组缓冲区，再恢复任务前完整快照；恢复失败或实际值不一致时保留待恢复记录。
- 临时解除限速前显示当前数值，区分总限速和 fq 单连接限速，并换算为 Mbps；无限制时显示“未设置”，不声称解除了已有上限。

HTTP 协议回归检查 IPv4/IPv6 下均分别显示两类接入命令，并标明 Linux / OpenWrt / iStoreOS 和 Windows PowerShell 5.1/7。每条都是单行，只包含一份 URL 和 token，不混入另一系统的执行分支，且不包含 `sudo`、Bash 或 Python 启动依赖。在本机已有的 sh、Bash、dash 中，Linux 命令直接执行或通过标准输入传入时，均通过真实 IPv4/IPv6 HTTP 下载 Shell 脚本，由服务器准确补齐四个参数，并保留成功和失败退出码；临时目录不提供 PowerShell 脚本，用于检查未下载错误文件。短链接还检查错误 token、过期、已结束、已关闭和已配对任务的拒绝行为，重复下载不会消耗配对机会，不生成凭据文件；原有 `/join.sh`、`/join.ps1` 仍可下载不含参数的原始脚本。替代下载工具用可执行的残缺内容验证 curl 失败时丢弃内容并回退 wget，两次下载均失败时不执行脚本。纯延迟任务不会启动 iperf3，重复组的采样结果有独立编号且正确保存；不接受该任务的吞吐或满载延迟回报，关闭纯延迟任务无需结束测速进程。

自动测试的数据只用于验证程序行为，不能作为线路优化效果的证据。

### 0.18.2 本地验证（2026-09-14）

Windows 本机在 `tests` 目录执行 `python -B -X utf8 -m unittest test_transport test_windows_client test_ports test_return_buffers -q`，117 项全部通过，无跳过，耗时 128.909 秒。sh、Bash、dash 均通过 IPv4/IPv6 下的 Linux 单行命令直接执行、标准输入、脚本参数补齐和退出码检查。PowerShell 5.1/7 均通过 Windows 单行命令的单次下载、默认代理绕过、过期下载失败退出、中文解码、配对、测速和退出清理检查。两类指令的显示、端口保留和调优流程回归均通过。

固定示例地址 `192.0.2.1`、接入端口 12223 和 32 字符示例 token，Linux / OpenWrt / iStoreOS 命令为 143 字符，Windows 命令为 133 字符；IPv6 示例 `2001:db8::1` 分别为 149 和 137 字符。每条命令只包含一份 URL 和 token，不含换行或 `sudo`，按系统复制对应的一条即可。

Python、Shell 和 PowerShell 语法检查、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。四个运行模块统一为 0.18.2，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成、OpenWrt/iStoreOS 真机测试或实际线路测速。

### 0.18.1 本地验证（2026-09-14）

Windows 本机在 `tests` 目录执行 `python -B -X utf8 -m unittest test_transport test_windows_client test_ports -v`，45 项全部通过，无跳过，耗时 139.915 秒。sh、Bash、dash 均通过单行命令的直接执行、标准输入、脚本参数补齐和退出码检查；PowerShell 5.1/7 均通过单行粘贴、中文解码、IPv4/IPv6 接入、测速与退出清理检查。下载失败、残缺内容回退、配对状态和端口保留检查均通过。

固定相同的示例 IPv4 地址、端口和 32 字符 token，接入命令由 349 字符缩至 338 字符，不含换行。Python、Shell 和 PowerShell 语法检查、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。四个运行模块统一为 0.18.1，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成或实际线路测速。

### 0.18.0 本地验证（2026-09-13）

Windows 本机执行 `python -B -X utf8 -m unittest discover -s tests -v`，共 217 项，耗时 168.854 秒：200 项通过，17 项 Linux 隔离集成测试因平台和启用条件跳过。Git for Windows 的 sh、Bash、dash 均通过通用短命令的整段执行、标准输入、四个参数补齐和退出码检查；PowerShell 5.1/7 均通过通用命令、标准输入、中文解码、IPv4/IPv6 接入、任务锁及退出清理检查。短链接的有效期、重复下载、配对后失效，以及下载失败和残缺内容回退检查均通过。

使用替代 sysctl 手动检查菜单的收发上限显示，覆盖不同收发值、不足 1 MiB、单项缺失和无效数据。固定相同的示例 IPv4 地址、端口和 32 字符 token，通用命令由 519 字符缩至 349 字符，减少 32.8%。

Python、Shell 和 PowerShell 语法检查、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。四个运行模块统一为 0.18.0，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成或实际线路测速。

### 0.17.0 本地验证（2026-09-13）

Windows 本机在 `tests` 目录执行 `python -B -X utf8 -m unittest test_transport test_windows_client test_entry test_return_buffers -v`，120 项全部通过，无跳过，耗时 138.417 秒。Git for Windows 的 sh、Bash、dash 均通过通用命令的整段执行和标准输入检查，真实 HTTP 下载准确传入四个参数，并保留成功及失败退出码。PowerShell 5.1/7 均通过整段执行和逐行输入的下载、配对检查；命令内保留空行，末尾无需额外补空行。IPv4/IPv6 接入、中文编码、客户端退出清理、原有菜单入口和缓冲区试调回归均通过。

Python、Shell 和 PowerShell 语法检查、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。四个运行模块统一为 0.17.0，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成或实际线路测速。

### 0.16.2 本地验证（2026-09-13）

Windows 本机在 `tests` 目录执行 `python -B -X utf8 -m unittest test_entry test_return_buffers test_transport -v`，共 93 项，耗时 186.373 秒：92 项通过，1 项仍匹配旧日志文案而失败。同步该完整流程用例的断言后单独复测，0.902 秒通过；两轮合计 93 项通过，无跳过。覆盖入口参数传递、缓冲区上调与下调、回退和完整流程，以及同版本 HTTP 接入。

Python、Shell 和 PowerShell 语法检查、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。确认指定启动文案已删除，试调日志使用收发缓冲区上限“各增加／减少”的容量。四个运行模块统一为 0.16.2，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成或实际线路测速。

### 0.16.1 本地验证（2026-09-13）

Windows 本机在 `tests` 目录执行 `python -B -X utf8 -m unittest test_entry test_return_records test_return_buffers test_transport -v`，108 项全部通过，无跳过，耗时 237.858 秒。覆盖默认 2 次、命令行和交互设置 1 次、上限 10 次及无效输入拒绝；运行前校验接受 1 次，单次测量正常记录、计算指标并参与候选判定，不会被当成未完成验证。原有缓冲区搜索、失败恢复及同版本配对回归也全部通过。

Python、Git for Windows 的 Shell 及 PowerShell 语法检查、五个发布文件的 SHA-256 校验和 `git diff --check` 均通过。运行模块统一为 0.16.1，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成或实际线路测速。

### 0.16.0 本地验证（2026-09-13）

Windows 本机执行 `python -B -X utf8 -m unittest discover -s tests -v`，共 209 项：首轮 188 项通过，17 项因需要 Linux 隔离挂载或网络环境而跳过，4 项未通过。保存失败恢复用例触发 Bash 20 秒超时；PowerShell 5.1 的直接脚本和纯延迟用例触发 25 秒超时，强制关闭用例未在 10 秒内启动替代测速进程。这四项随后使用原有超时值独立复测，23.134 秒全部通过，未为复测修改代码或超时值；两轮合计 192 项通过、17 项跳过。

68 项优化线路缓冲区专项全部通过，覆盖 `1.5 × BDP` 初值、`2.5 × BDP` 上限、低于 6 MiB 和不足 4 KiB 的参数、精确落在非整数 MiB 上限、无提速仍保留更大值、1% 重传边界、5% 固定速度保护、高重传后微调、最终保存与队列复测。缓冲区实际写入回归还验证小字节候选的默认值与最小值、超过 1 GiB 的合法候选及整数越界拒绝。PowerShell 5.1 和 7 的客户端回归均已通过。

Python 与 Shell 语法、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。运行模块统一为 0.16.0，保持 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未执行 Linux 隔离集成或实际线路吞吐对比。

### 0.15.0 本地验证（2026-09-13）

Windows 本机执行 `python -B -X utf8 -m unittest discover -s tests -v`，共 199 项：首轮 181 项通过，17 项因需要 Linux 隔离挂载或网络环境而跳过，1 项菜单显示用例触发 Bash 进程 20 秒超时。该菜单用例随后独立复测，耗时 9.643 秒通过，未修改测试或超时值；两轮合计 182 项通过、17 项跳过。

回归验证初值波动后持续试调、首次稳定候选建立固定参照、三轮无收益后继续搜索、超过八轮和三十分钟仍可运行，以及候选数据缺失恢复、初值不稳定后的队列复测、最终保存和异常恢复。PowerShell 5.1 和 7 的客户端回归均通过。入口与主菜单均已清除旧轮数和时长限制文案。

Python 与 Shell 语法、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。运行模块统一为 0.15.0，使用 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未运行 Linux 隔离集成或实际线路吞吐对比。

### 0.14.0 本地验证（2026-09-13）

Windows 本机执行 `python -B -X utf8 -m unittest discover -s tests -q`，共 192 项：175 项通过，17 项因需要 Linux 隔离挂载或网络环境而跳过，无失败。覆盖纯空载延迟采集、先应用初值再测速、取消原始基线与试调结束独立验收、按实际参数合并结果及序号选择、当前限速显示，以及队列变化后的验证与失败恢复。PowerShell 5.1 和 7 的 22 项客户端回归均通过。

Python 与 Shell 语法、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过。运行模块统一为 0.14.0，使用 UTF-8、LF 换行，Windows 测速脚本保留 UTF-8 BOM。本轮未运行 Linux 隔离集成或实际线路吞吐对比。

### 0.13.1 本地验证（2026-09-12）

Windows 本机执行 `python -B -X utf8 -m unittest discover -s tests -v`，共 177 项：160 项通过，17 项因需要 Linux 隔离挂载或网络环境而跳过，无失败。其中 50 项缓冲区专项用例验证 2 MiB 上探、高重传后从触发候选减 1 MiB、微调失败回退、跳过已测上限、阈值边界以及完整流程的独立验收。PowerShell 5.1 和 7 的测速端回归也通过。

Shell 语法、五个发布文件的 SHA-256 校验及 `git diff --check` 均通过，运行模块版本统一为 0.13.1。本轮未运行 Linux 隔离集成或实际线路吞吐对比。

### 0.13.0 本地验证（2026-09-12）

Windows 本机执行 `python -B -X utf8 -m unittest discover -s tests -v`，共 172 项：155 项通过，17 项因需要 Linux 隔离挂载或网络环境而跳过，无失败。覆盖重传优秀后的连续上调、3% 提速边界、测速区间重叠、无收益回退后继续探测、试调上限、独立验收和异常恢复；PowerShell 5.1 与 7 的测速端回归也通过。

`tcpfit.sh`、`install.sh` 和 `tcpfit-client.sh` 的 Shell 语法检查、五个发布文件的 SHA-256 校验以及 `git diff --check` 均通过。运行文件版本统一为 0.13.0，使用 UTF-8、LF 换行；Windows 测速脚本保留 UTF-8 BOM。本轮未运行 Linux 隔离集成或服务器到家宽的实际吞吐对比。

## Linux 隔离集成

需要 Linux root、Python 3、Bash、iproute2、util-linux、curl、wget 和 iperf3。完整防火墙验证还需要系统已有的 iptables/ip6tables、nftables 和 UFW；缺少对应工具的用例会跳过，测试不安装软件。必须在独立网络命名空间中运行；UFW 共存用例还需要独立挂载命名空间。

```bash
sudo env TCPFIT_LINUX_INTEGRATION=1 unshare --mount --net \
  python3 -m unittest discover -s tests -v
```

集成测试只创建临时虚拟网卡与本地测试端口，覆盖以下行为：

- 单层 HTB + fq、定制 fq、fq_codel、mq 各叶子的保存和恢复。
- 只有已配对来源可以访问测试端口，删除临时规则后不残留任务链。
- 分别验证 nftables、iptables/ip6tables 的 IPv4 和 IPv6 来源限制：两个端口原本被拒绝时，接入端口临时可达，配对后测速端口可达；撤销本次规则后重新受原规则限制。
- UFW 启用时复用其现有底层工具，重载后仍限制测速来源，退出后保留原配置。UFW 的启用、重载和关闭只在隔离网络与配置副本中执行。
- 无防火墙工具时，真实 iperf3 客户端完成单连接、四连接反向测速和结果回报；流量仅在隔离环境的本地回环中传输。
- 只有 curl 或只有 wget 时，均可通过 HTTP 下载接入脚本并正确传递四个参数。
- 国际线路调优入口与手动恢复遵守相同任务锁。
- 等待接入和 iperf3 测速服务运行期间分别发送 SIGINT，检查服务退出、两个端口可重新绑定、临时防火墙规则已清理。
- 协调进程被 SIGKILL 后，守护进程恢复队列、清理端口规则、凭据与待恢复标记。
- 测速端被 SIGKILL 后，独立清理进程撤销本地凭据和任务锁，并报告异常退出。
- IPv6 路由到期倒计时变化不触发无意义回写；确需回写时使用 iproute2 接受的有效期格式。

还可同时检查安装、模块定位、校验失败时保留原入口以及完整更新。此测试另外隔离挂载命名空间，在临时文件系统中安装程序，以本地样本代替远程下载：

```bash
sudo env TCPFIT_INSTALL_INTEGRATION=1 TCPFIT_LINUX_INTEGRATION=1 \
  unshare --mount --net python3 -m unittest discover -s tests -v
```

## 双端实测

只验证接入、认证、反向下载和结果回报，可在常规 Linux 服务器执行：

```bash
sudo python3 tests/peer_smoke.py --server <服务器可达地址>
```

在另一端按系统复制对应的一条接入命令、一次粘贴；Linux / OpenWrt / iStoreOS 在 Shell 中执行，Windows 在 PowerShell 中执行。该检查分别运行短时单连接和四连接下载，结束后清理自己的端口规则；不应用系统调优参数。要验证完整流程，使用 `bash tcpfit.sh return`。

完整实测必须另外保存服务器现状，并在测试结束后按约定保留或恢复。核对运行参数、持久化文件、队列、默认路由、服务状态以及两端的临时进程和文件。路由比较应排除自动路由剩余寿命等动态字段。测速端缺少 tc 时，可通过 `ip -d link show` 核对队列类型，不要为了检查而改它的队列。

实机原始记录包含地址、配置和测量数据，留在设备的私有目录中，不提交到源码仓库。吞吐变化只证明当时条件下的测量差异；是否保留参数，以任务记录的完整吞吐和重传判定为准。

### 0.12.0 Windows 测速端验证（2026-09-12）

Windows 10 测速端使用 iperf3 3.21，Debian 12 调优端使用 iperf3 3.12。分别通过 PowerShell 5.1 和 7.6.3 执行调优端生成的接入命令，以 IPv4、TCP 12223 / 12224 完成反向下载。每项运行一次、持续 4 秒，服务器记录如下：

| PowerShell | 连接数 | 接收 Mbps | 服务器重传次数 | 估算重传比 |
|---|---:|---:|---:|---:|
| 5.1 | 1 | 342.72 | 0 | 0.000% |
| 5.1 | 4 | 446.47 | 44582 | 27.348% |
| 7.6.3 | 1 | 196.48 | 3803 | 5.418% |
| 7.6.3 | 4 | 448.08 | 42063 | 25.950% |

四轮各取得 5 个有效空载、3 个有效满载 TCP 握手样本，配对、版本检查、接收速度与服务器重传解析、结果回报均通过，两端正常退出。这些短时测量用于确认协议兼容，吞吐与重传受当时线路状态影响；本轮未执行参数试调。

测试复用了服务器已有 UFW 底层规则。结束后对比 TCP 收发参数、拥塞控制、默认队列类型、实际队列、IPv4/IPv6 默认路由以及 nftables、iptables/ip6tables 规则，均与测试前一致，比较时排除了计数器和动态路由有效期。两个测试端口均可重新绑定，服务器无残留 iperf3 进程或双端检查临时目录。服务器补装的 iperf3 及其依赖保留，常驻 iperf3 服务未启动。

本版自动回归共 167 项：Windows 本机通过 150 项，跳过 17 项 Linux 专用测试；Linux 隔离挂载与网络环境通过 149 项，跳过 18 项 Windows 专用测试。两种环境合计覆盖全部测试，其中包括 Windows 脚本安装、损坏文件拒绝更新、版本不匹配拒绝配对、重复任务拒绝和强制关闭后的清理。Shell 语法检查及五个发布文件的 SHA-256 校验均通过。

### 0.8.1 实测记录（2026-09-11）

美西 4837 线路服务器到广东移动家宽，IPv4 反向下载；填写服务器 5000 Mbps、家宽 1000 Mbps。服务器可见内存约 1960 MiB，原收发上限均为 61.25 MiB。实测路径带宽取整为 650 Mbps，推导参考带宽为 1000 Mbps，空载握手延迟推导值为 215 ms。因此 BDP 为 26,875,000 字节，按 `1.5 × BDP + 2 MiB` 得到 42,409,652 字节，约 40.44 MiB。

以下速度和估算重传比均分别取该阶段三次测量的中位数；上限为接收、发送各自的值。

| 阶段 | 上限 MiB | 单连接 Mbps | 单连接重传比 | 四连接 Mbps | 四连接重传比 | 处理 |
|---|---:|---:|---:|---:|---:|---|
| 调优前 | 61.25 | 474.22 | 0.746% | 530.57 | 15.173% | 比较基准 |
| 基础候选 | 40.44 | 253.33 | 4.898% | 637.05 | 13.155% | 继续试调 |
| 第 1 轮，下调 2 MiB | 38.44 | 538.65 | 0.000% | 610.69 | 9.919% | 保留候选 |
| 第 2 轮，下调 2 MiB | 36.44 | 543.16 | 0.000% | 600.89 | 14.737% | 回退 |
| 第 3 轮，下调 1 MiB | 37.44 | 505.99 | 0.000% | 605.18 | 10.787% | 回退 |
| 第 4 轮，反向上调 1 MiB | 39.44 | 557.36 | 0.005% | 678.36 | 9.788% | 保留候选，通过最终比较 |

最终候选为收发各 41,361,076 字节，约 39.44 MiB，默认值保持各 1 MiB。单连接、四连接速度中位数分别比调优前提高 17.53%、27.85%；搜索因两侧 1 MiB 邻点均已测过而停止，没有创建全局整形。四连接重传仍高，本轮对比也包含完整基础调优，不能把差异全部归因于 BDP 倍率，或据此确定其他时段的最佳参数。512 MiB 档的 24 MiB 上限由自动回归验证，本轮服务器的物理内存不属于该档。

测试后使用独立快照恢复原收发上限及其他配置。逐项确认 33 项参数、持久化文件、默认路由、队列、防火墙、服务状态和已安装入口均与测试前相同；测速端参数、路由及队列也未改变，比较时排除了网桥动态计时器。临时测试端口、进程、凭据和待恢复标记均已清理。

恢复过程中发现自动 `fq 0:` 队列已重建后再次执行 `change` 会报错，已修复并在该服务器上完成恢复复核。修复后的本地回归共 111 项，95 项通过、16 项因环境跳过；另在 Linux 独立网络命名空间中通过 HTB + fq、自定义 fq、自定义 fq_codel 和 mq 四项队列恢复测试。

## 发布文件

版本号在 `tcpfit.sh`、`tcpfit-return.py`、`tcpfit-client.sh`、`tcpfit-client.ps1` 和 README 中保持一致。使用 UTF-8、LF 换行；`tcpfit-client.ps1` 保留 UTF-8 BOM，使 PowerShell 5.1 直接读取文件时也能正确处理中文，其他运行文件不带 BOM。重新生成并检查：

```bash
sha256sum tcpfit.sh install.sh tcpfit-return.py tcpfit-client.sh tcpfit-client.ps1 > SHA256SUMS
sha256sum -c SHA256SUMS
```

发布 `v<版本号>` 标签时，Release 附件应包含上述五个运行及安装文件和 `SHA256SUMS`。更新命令从 Release 下载；缺文件或校验失败会停止安装或更新。测速端从当前调优服务器获取脚本，不依赖 Release 下载。
